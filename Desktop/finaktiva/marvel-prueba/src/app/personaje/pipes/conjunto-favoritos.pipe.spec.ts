import { ConjuntoFavoritosPipe } from './conjunto-favoritos.pipe';

describe('ConjuntoFavoritosPipe', () => {
  it('create an instance', () => {
    const pipe = new ConjuntoFavoritosPipe();
    expect(pipe).toBeTruthy();
  });
});
